{
	"name": "@virtual-planet/schema",
	"version": "0.0.0",
	"private": true,
	"description": "Schema/type-system extension for the scene model: a functional, plain-data layer over TypeBox (introspectable JSON-Schema + static inference) plus domain annotations (units, extents, path refs).",
	"repository": {
		"type": "git",
		"url": "https://github.com/saabi/world-lab.git",
		"directory": "packages/schema"
	},
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "vitest run"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.9"
	},
	"dependencies": {
		"@sinclair/typebox": "^0.34.49"
	}
}
